#include<stdio.h>
int main()
{
    int i,sum=0;
    for(i=1;i<=100;i++)
    {
        if(i%3 != 0)
        {
            continue;
        }
        printf(" %d\n",i);
sum+=i;
    }
    printf("sum of all integer divisible by 3 is : %d",sum);
     return 0;
}
